export const metadata = {
  title: "Request Commercial Pool Service",
  description:
    "Request a commercial pool walkthrough quote for apartments, hotels, schools, and gyms in Fresno, Clovis, and the Central Valley.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
